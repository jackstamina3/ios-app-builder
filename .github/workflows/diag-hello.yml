# Temporary diagnostic workflow - remove after Actions setup is confirmed.
name: diag-hello

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  hello:
    runs-on: ubuntu-latest
    timeout-minutes: 2
    steps:
      - name: Hello
        run: echo "actions-runner-ok on $(uname -a)"
